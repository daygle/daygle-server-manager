# Project Title

Short project description...

## Features

- Remote Linux updates over SSH with status reporting
- Web UI for managing servers and reviewing update history/status
- Scheduled updates using cron expressions (enable/disable/delete schedules)
- Background worker execution for long-running update jobs
- SSH key management (generate/import keys; copy public key to targets)
- Role-based admin pages for managing users and SSH keys
- Docker Compose deployment model (api/worker/db services)
- PostgreSQL-backed persistence

...rest of the README content unchanged...